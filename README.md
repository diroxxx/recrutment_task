# recrutment_task

A Spring Boot REST API that fetches GitHub repository information for a given user.

## Endpoint

`GET /api/repos/{username}` — returns a list of the user's repositories with their branches and latest commit SHAs.

## Running

```bash
./gradlew bootRun
```
